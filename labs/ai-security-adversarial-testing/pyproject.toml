[project]
name = "adversarial-prompt-engineering-lab"
version = "0.2.0"
description = "Twelve-test deterministic prompt architecture, adversarial evaluation, mitigation, versioning, and regression lab"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
