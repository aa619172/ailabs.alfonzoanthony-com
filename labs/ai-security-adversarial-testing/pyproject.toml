[project]
name = "adversarial-prompt-engineering-lab"
version = "0.2.0"
description = "Deterministic prompt architecture, adversarial evaluation, mitigation, and regression testing lab"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
